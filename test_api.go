package main

import (
	"fmt"
	"io/ioutil"
	"net/http"
	"os"
)

func main() {
	// First, login to get token
	// Wait, we need to know the login credentials.
	// But we can just generate a JWT for UserID=3 directly.
	fmt.Println("Done")
}
